// import { httpRequest } from "@/shared/utils/http-client";
// import type { IResponseGlobal } from "@/shared/types/standard-response";

// export async function Logout(user_id:string): Promise<string> {
//   return await httpRequest<IResponseGlobal<string>>(`${import.meta.env.VITE_API_KEY}logout/${user_id}`, {
//     method: "PATCH",
//   });
// }
